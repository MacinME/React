
export const reducer = ( initialState, action) => {
    switch ( action.type ) {
        case 'NONE':
            throw new Error('Not implemented yet');
        default:
            return state;
    }
}